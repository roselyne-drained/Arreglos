//
// Created by usuario on 23/11/2024.
//
#include <stdio.h>

int main(void) {
    int arreglo[10];
    for (int i = 0; i < 10; i++) {
        printf("Ingresa un numero:\n");
        scanf(" %d", &arreglo[i]);
        if (arreglo[i] < 0) {
}